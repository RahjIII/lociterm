------- TODO TODO TODO TODO --------------

* C locally saved (on browser) prefs

* C Get the old login window working.

* C make the "about" menu item show credits.

* C adjust the lociterm applytheme to be able to re-style the normal css stuff.

* C configurable client element and text sizes.

* M add a "tar up the distro" method to top level make?

* M add a better "install me" option to make

* S systemd/sysv launcher script?

* S lws_daemonize check that.

* S drop perms after launch?

* S Get the terminal types and mtts values to be selectable from the config file.

* S Get the connection endpoint to be selectable from the config file.

* S Get https: and wss: working!

* S Optional- telnet-ssl on the game side, selectable from config?

* C client option for keep keyboard open

* S get it working through apache ws proxy.

* S fix the locid_log function to work better at showing the connection the messages are for.

------------- DONE! -----------------
* configurable client themes/colors into menuside code too.

+ configurable client themes/colors 

+ Figure out the "rendering before fonts loaded" thing.

+ Get the intial styling of the client worked out better.

+ Create a top level makefile to build a packageable dist of client and server.

+ add in 'the usual' license credits and readme's

+ Get it into cvs.

+ clean up file headers for initial checkin.

+ Get NEW-ENVIRON working to support MNES standard variables
	report IPADDRESS
	CHARSET
	CLIENT_NAME
	CLIENT_VERSION
	MTTS
	TERMINAL_TYPE

- Check on charset support.? Naw...  LO handles the env var correctly.

- Check on eor support. meh not really needed for LO.

